% Get from CSV
% Entered into SQL
function output = memorizeCSV(table_name, csv_location)
    X = dlmread(csv_location, ',', 1, 0);
    X_size = size(X);
    for (i=1;i
    fid = fopen(csv_location, 'r');
    fields = textscan(fid, '%[^,],%[^,],%[^,\r\n]', 1);
    fclose(fid);
    
    memorize(table_name, fields, X);
    output = true;
end